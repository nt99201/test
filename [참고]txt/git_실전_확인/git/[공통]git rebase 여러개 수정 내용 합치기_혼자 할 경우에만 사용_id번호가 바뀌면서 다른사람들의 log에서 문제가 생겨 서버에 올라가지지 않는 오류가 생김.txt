git rebase -i HEAD~3 (병합처리하러 불러올 갯수)

insert키는 텍스트 타입으로 바꿔줌(하단에 -- INSERT -- 라고 나오면 텍스트 수정가능해짐)
esc
:wq
w(저장) q(종료)


맨 위에껀 전체 합친 최종 소스 네이밍 수정

아래것 합쳐질 최종 소스 네이밍 수정

insert키는 텍스트 타입으로 바꿔줌(하단에 -- INSERT -- 라고 나오면 텍스트 수정가능해짐)
esc
:wq

git add .

$ git status


$ git log --oneline --decorate --graph -30

$ git status

$ git commit -m ""

$ git push origin KINSUS-25139 -f
(-f를 사용할 경우에는 아이디 정렬 번호가 안맞을경우에 억지로 맞추기 위해 쓴다, 필요할때만 사용한다,)



[참고]
a → 내용수정?
insert 키 → 내용수정!
ctrl+z → 내용 빠져 나오기